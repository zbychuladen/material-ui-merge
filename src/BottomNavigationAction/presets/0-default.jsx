import * as React from 'react';
import BottomNavigationAction from '../BottomNavigationAction';

export default (
  <BottomNavigationAction uxpId="1" label="Recents" showLabel icon={"<Icon>restore</Icon>"} />
);