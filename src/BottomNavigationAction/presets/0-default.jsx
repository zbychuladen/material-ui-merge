import * as React from 'react';
import BottomNavigationActionM from '../BottomNavigationAction';

export default (
  <BottomNavigationActionM key="1" label="Recents" showLabel icon={"<Icon>restore</Icon>"} />
);