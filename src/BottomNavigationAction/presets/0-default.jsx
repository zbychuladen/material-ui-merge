import * as React from 'react';
import BottomNavigationAction from '../BottomNavigationAction';
import Icon from '../../Icon/Icon';

export default (
  <BottomNavigationAction uxpId="1" label="Recents" showLabel icon={<Icon uxpId="1.1">restore</Icon>} />
);
