import * as React from 'react';
import Chip from '../Chip';

export default (
  <Chip
    key="1"
    avatar={
      `<Avatar>
        <Icon>face</Icon>
      </Avatar>`
    }
    label="Clickable Deletable Chip" />
);