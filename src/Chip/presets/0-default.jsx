import * as React from 'react';
import ChipM from '../Chip';

export default (
  <ChipM 
    key="1"
    avatar={
      `<Avatar>
        <Icon>face</Icon>
      </Avatar>`
    }
    label="Clickable Deletable Chip" />
);