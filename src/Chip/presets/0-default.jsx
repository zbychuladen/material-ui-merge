import * as React from 'react';
import Chip from '../Chip';

export default (
  <Chip
    uxpId="1"
    avatar={
      `<Avatar>
        <Icon>face</Icon>
      </Avatar>`
    }
    label="Clickable Deletable Chip" />
);